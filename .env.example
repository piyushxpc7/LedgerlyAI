# Ledgerly Environment Configuration
# Production: set ENVIRONMENT=production and all required vars. No localhost in production.

# --- Required in production ---
ENVIRONMENT=development
# Production: use "production". Then DATABASE_URL (PostgreSQL), JWT_SECRET (32+ chars), CORS_ORIGINS or FRONTEND_URL are required.

# Database (PostgreSQL required in production)
DATABASE_URL=postgresql://ledgerly:ledgerly_secret@localhost:5432/ledgerly

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT Secret - Production: min 32 characters, never use default
JWT_SECRET=change-this-in-production-to-a-long-random-string

# CORS - Production: comma-separated frontend origins (no localhost). If unset, FRONTEND_URL is used.
# CORS_ORIGINS=https://app.example.com

# Frontend URL (used when CORS_ORIGINS unset; OAuth redirects)
FRONTEND_URL=https://app.example.com

# Backend base URL (for OAuth redirect URI when not using GOOGLE_OAUTH_REDIRECT_URI)
API_BASE_URL=https://api.example.com

# --- Google OAuth ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Production: set to exact redirect URI registered in Google Console (e.g. https://api.example.com/auth/google)
GOOGLE_OAUTH_REDIRECT_URI=

# --- LLM ---
LLM_PROVIDER=mistral
MISTRAL_API_KEY=your-mistral-api-key
ANTHROPIC_API_KEY=your-anthropic-api-key

# --- Storage ---
STORAGE_PATH=./storage
